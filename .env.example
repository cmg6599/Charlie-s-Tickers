# Copy this file to `.env` in the same folder as `server.js`:
#   cp .env.example .env
# Then put your real Finnhub token on the FINNHUB_API_KEY line in `.env`.
# Restart `node server.js` after any change.
#
# https://finnhub.io/register
FINNHUB_API_KEY=
